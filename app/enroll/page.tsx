// Preview page — wires EnrollmentForm for visual review before Step 3 (API + Supabase)
import EnrollmentForm from '@/components/enrollment/EnrollmentForm'

export default function EnrollPage() {
  return <EnrollmentForm />
}
